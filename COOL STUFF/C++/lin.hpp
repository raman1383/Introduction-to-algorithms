#pragma once
#include <iostream>
constexpr int MAX = 100;

namespace lin
{
    void fucker()
    {
        for (int i = 0; i < MAX; i++)
        {
            std::cout << "Lets fuck >>>" << std::endl;
        }
    }

} // namespace lin
